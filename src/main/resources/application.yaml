spring:
  profiles:
    active: dev,mock

---

spring:
  profiles: dev
  datasource:
    url: jdbc:h2:file:./db/rdbms;AUTO_SERVER=TRUE
    driver: org.h2.Driver
    username: sa
    password:
    hikari:
      maximumPoolSize: 2
server:
  port: 9090

flyway:
  url: jdbc:h2:file:./db/rdbms;AUTO_SERVER=TRUE
  schemas: PUBLIC
  user: sa
  password:
